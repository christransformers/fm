# Fuse Music working preferences

- This is the static GitHub Pages site at `christransformers/fm`; keep `CNAME` as `www.fusemusic.com.au`.
- For substantial tasks, delegate a plan and checklist to `.codex/agents/planner.toml` and bounded repository lookups or cleanup to `.codex/agents/grunt.toml`. Wait for their reports, then make final product and architecture calls yourself. Give grunt explicit read-only or edit scope.
- Keep the current session's model and reasoning effort stable. Do not claim an exact context-token count, 272,000-token threshold, current account usage, five-hour or weekly percentage, reset time, or model breakdown without a meter that actually exposes it.
- After a task, briefly report available usage facts and the most likely source of avoidable token use. Prefer targeted file reads and compact tool output over dumping entire files or histories.
- The player is a static, local catalogue with browser-stored preferences and playlists. Do not imply access to a licensed global music catalogue or user accounts.
